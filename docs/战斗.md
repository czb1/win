# 战斗模块设计

本文是战斗模块唯一的长期设计文档。以后战斗策略、武器选择、目标筛选、弹道与伤害估算的变更都直接更新本文，不新建设计文档。

## 目标与边界

- 首要目标是只攻击威胁我方基地的机器人，并在合法动作、响应时限和存活之间取平衡。
- 战斗结算、隐藏信息与机器人行为由官方引擎负责；本模块只基于当回合可见状态生成动作。
- 经济、建墙和任务求解分别由其他模块负责，战斗模块只消费它们产生的建筑、角色与资源状态。

## 当前策略

- 通过 `targetTeam` 识别威胁我方的机器人；字段缺失时才以双方基地距离做兼容推断。
- 存活角色与武器之间预计算操作位最短路，并在最多三名角色、三座武器的范围内枚举最短总路程分配。
- 操作位先选择靠基地内侧、远离当前威胁的位置，再比较返程距离，降低破口后的操作者死亡概率。
- 加特林使用直射弹道和锥形目标约束；电磁炮沿射线按剩余能量估算穿透收益；火箭按中心与邻格溅射估算收益。
- 炮塔、角色和范围物品都执行阵营隔离，避免攻击对方一路的机器人；没有安全弹道时宁可少开火。
- 攻击前统一经过指令账本校验：昼夜、射程、冷却、操控者、目标数量、坐标与单角色单动作约束。

## 关键设定与原因

| 设定 | 当前值或规则 | 原因 |
|---|---|---|
| 武器数量 | 最多三座 | 遵守规则上限，也使角色与武器分配可小规模穷举 |
| 加特林夹角 | 任意两目标方向点积不小于 0 | 保证输出目标落在不超过 90° 的锥形范围 |
| 火箭伤害估算 | 中心 20、邻格 10 | 延续当前协议与实现假设；仍需官方引擎校准 |
| 大型近身权重 | 大型 `+4`、Boss `+8`，距基地不超过 2 时加倍 | 避免高生命目标长期贴近基地持续输出 |
| 电磁收益 | 沿线按距离与剩余生命分配能量 | 避免把穿透伤害重复计算到已被计划击杀的目标 |
| 不安全弹道 | 放弃攻击 | 避免帮助对手清理其来敌，优先保证阵营隔离 |

## 已知风险与验证

- 火箭只评估当前机器人格，可能遗漏更优空格落点。
- 多武器同回合的过杀、穿透和实际结算顺序仍是近似。
- 缺失 `targetTeam` 时的距离推断只是兼容路径，不应替代正式字段。
- 回归应覆盖三类武器、各等级目标数、遮挡、冷却、镜像地图、两路混合机器人与大波次耗时。

## 变更记录

### 2026-09-16 - 两名角色轮换操控三座火箭

- 策略：夜前为开拓者安排同时邻接两炮的站位，另一名工人操控第三炮；第三名工人保留独立回防站位，接管完成且有安全经济路线时才释放采矿。两炮同时就绪时按当前目标的威胁加权伤害选择，遵守每名角色每回合一次操控。
- 设定：`night_economy_enabled` 默认开启，仅适用于存活的三座火箭、健康开拓者与两名健康工人；没有可达共享站位、开拓者仍执行任务、操控者用药或未完成交接时沿用普通操控分配。
- 原因：火箭有三个冷却空窗，错开两炮的发射回合能让一名角色维持两炮持续输出，释放一名工人增加资源获取；不改变现有炮塔和城墙坐标。
- 经验教训：同时就绪的两炮仍需分两回合发射，首次爆发会延迟；共享站位还必须给采矿工人保留实际可通行的出口，不能只检查邻接距离。基地受击、墙体破口或敌人进入返程预算时恢复三人防守。
- 验证：仓库 Python 3.11 `.venv` 全套 208 项测试通过，其中新增 16 项夜间经济回归。连续 12 回合场景中两人操控三炮，每炮发射三次、间隔四回合，同时工人至少采集八次并出售五单位矿石；默认与镜像布局回防、角色动作互斥、危险召回和经济规划超时回退均通过。既有建设与升级基准均无非法动作，740 个静态机器人的密集战斗基准保持三炮开火、最慢决策 39.27 毫秒；正式引擎的夜间采集合法性和实战资源提升仍需对战验证。

### 2026-09-16 - 建立四模块文档基线

- 策略：把既有战斗分配、阵营隔离、弹道和伤害估算归并到本文件。
- 设定：本文件成为战斗设计的唯一维护入口，后续不再新建战斗设计文档。
- 原因：降低文档分散造成的策略冲突，让实现、取舍和验证边界可以在同一处审阅。
- 经验教训：合法动作不等于高收益动作；阵营识别、实际命中路径和冷却成本必须一起评估。
- 验证：本次为文档与门禁变更；由 `design-governance` 检查文档结构和更新纪律。

### 2026-09-16 - 夜间战斗诊断

- 策略：逐夜间回合记录基地、敌人、墙体和炮塔状态，并关联上一回合攻击结果与防御动作反馈。
- 设定：夜间及 tick 0、69 输出 battle_state；记录炮塔等级、火力、射程、冷却、操控者、目标和未开火原因，近基地敌人明细最多 12 个。
- 原因：区分缺乏操控者、正在赶路、冷却、射程不足和无安全目标等不同火力问题。
- 经验教训：只记录指令数量无法解释基地受损；必须把实际攻击与可观察障碍联系起来。
- 验证：PR #13 的 Python strategy checks（run 35053714092）已通过，其中第三夜诊断用例检查基地血量、墙体等级、冷却原因和操控者；日志不等于官方结算结果。

### 2026-09-16 - 夜间日志事件化

- 策略：把逐夜间回合的完整战场快照改为昼夜边界、每十回合、墙体变化和攻击失败时的紧凑摘要。
- 设定：附近敌人明细最多保留 4 个，墙体只记录数量、等级分布和明确受损数；成功防御动作只在下一回合合并记录一次，完整动作降为 DEBUG。
- 原因：完整比赛中重复墙体、炮塔和敌人数组会主导日志体积，妨碍下载和故障分析。
- 经验教训：诊断价值来自变化、失败和周期性心跳，不来自稳定状态的逐回合复制。
- 验证：新增稳定夜间不重复输出 INFO 的回归；原有第三夜用例继续检查基地血量、墙体等级、冷却原因和操控者。

### 2026-09-16 - 大型目标、内侧操作位与阶段摘要

- 策略：大型和 Boss 机器人贴近基地时提高边际伤害权重；操作位优先基地内侧与远离威胁。墙体变化改记 `wall_delta`，黄昏输出 `day_summary`，基地消失输出 `match_end`。
- 设定：完整 `battle_state` 只保留昼夜边界、十回合心跳和攻击失败；普通墙体损伤不再触发完整快照。日摘要包含基地/武器等级、金币、背包、墙体和下一升级阻塞原因。
- 原因：第三、第四夜均由大型机器人突破后杀死操作员并持续攻击基地；同时基地掉血阶段的重复完整快照占据大部分日志。
- 经验教训：高总溅射伤害不能替代对持续高伤目标的压制；日志应解释成长为何停滞，而不只是重复战场数组。
- 验证：新增大型近身权重、基地内侧操作位、墙体增量、黄昏摘要和基地消失回归；原有弹道、阵营隔离和操作员分配测试继续通过。
